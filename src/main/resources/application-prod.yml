spring:
  datasource:
    url: jdbc:mysql://localhost:3306/przepisy
    username: root
    password:
  liquibase:
    contexts: prod
  web:
    resources:
      static-locations: classpath:/static/,file:///C:/uploads/
app:
  storage:
    location: ./uploads/
